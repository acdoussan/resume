image: texlive/texlive:latest

stages:
  - build

build:
  stage: build
  script:
    - apt-get install fonts-font-awesome
    - cd examples
    - xelatex resume.tex
  artifacts:
    paths:
      - examples/resume.pdf
